apiVersion: v1
kind: PersistentVolumeClaim
metadata:
      name: myweb-pvc
      labels:
              env: production
spec:
      accessModes:
              - ReadWriteOnce
      resources:
              requests:
                      storage: 1Gi
